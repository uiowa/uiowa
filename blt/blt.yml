project:
  profile:
    name: sitenow

# uiowa:multisite:install invokes drupal:install, which only passes
# --existing-config to drush site:install when this is TRUE. BLT defaults it to
# FALSE, which would install the sitenow profile from scratch instead of from
# config/default.
cm:
  core:
    install_from_config: true
