adding:
  dependencies:
    config:
      - node.type.contact
  display:
    default:
      display_options:
        filters:
          type:
            value:
              contact: contact
removing: {  }
