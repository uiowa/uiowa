adding:
  dependencies:
    config:
      - node.type.contact
      - node.type.sign
  permissions:
    - 'enter contact revision log entry'
    - 'view contact revisions'
    - 'view sign revisions'
removing: {  }
