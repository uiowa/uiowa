adding:
  dependencies:
    config:
      - node.type.artwork
  display:
    default:
      display_options:
        filters:
          type:
            value:
              artwork: artwork
removing: {  }
