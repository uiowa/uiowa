adding:
  dependencies:
    config:
      - node.type.named_building
  type_settings:
    entity_types:
      node:
        - named_building
removing:
  dependencies:
    config:
      - node.type.alert
  type_settings:
    entity_types:
      node:
        - alert
