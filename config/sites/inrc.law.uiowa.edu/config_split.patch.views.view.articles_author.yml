adding:
  display:
    default:
      display_options:
        header:
          area:
            content:
              value: '<h2 class="h2 block__headline headline headline--serif headline--underline">Authored by {{ title }}</h2>'
removing:
  display:
    default:
      display_options:
        header:
          area:
            content:
              value: '<h2 class="h3 block__headline headline headline--serif headline--underline">Authored by {{ title }}</h2>'
