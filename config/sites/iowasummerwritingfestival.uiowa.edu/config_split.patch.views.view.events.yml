adding:
  display:
    card_list:
      display_options:
        fields:
          field_event_when:
            settings:
              format: default_long_date
removing:
  display:
    card_list:
      display_options:
        fields:
          field_event_when:
            settings:
              format: default
