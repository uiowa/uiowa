adding:
  dependencies:
    config:
      - node.type.service
  permissions:
    - 'enter alert revision log entry'
    - 'enter service revision log entry'
    - 'view service revisions'
removing: {  }
