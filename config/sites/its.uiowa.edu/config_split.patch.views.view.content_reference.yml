adding:
  dependencies:
    config:
      - node.type.service
  display:
    default:
      display_options:
        filters:
          type:
            value:
              service: service
removing: {  }
