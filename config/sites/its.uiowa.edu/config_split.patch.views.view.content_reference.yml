adding:
  dependencies:
    config:
      - node.type.service
  display:
    default:
      display_options:
        pager:
          options:
            pagination_heading_level: h4
        filters:
          type:
            value:
              service: service
removing:
  display:
    default:
      display_options:
        pager:
          options:
            pagination_heading_level: h2
