adding:
  dependencies:
    config:
      - node.type.service
      - node.type.support_article
  type_settings:
    entity_types:
      node:
        - service
        - support_article
removing:
  dependencies:
    config:
      - node.type.alert
  type_settings:
    entity_types:
      node:
        - alert
