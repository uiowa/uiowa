adding:
  dependencies:
    config:
      - node.type.service
  type_settings:
    entity_types:
      node:
        - service
removing:
  dependencies:
    config:
      - node.type.alert
  type_settings:
    entity_types:
      node:
        - alert
