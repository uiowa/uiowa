adding:
  dependencies:
    config:
      - node.type.resource
      - node.type.writer_bio
  permissions:
    - 'enter writer_bio revision log entry'
    - 'view resource revisions'
    - 'view writer_bio revisions'
removing: {  }
