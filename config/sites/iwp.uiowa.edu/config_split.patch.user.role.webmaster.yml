adding:
  dependencies:
    config:
      - taxonomy.vocabulary.writer_bio_session_status
  permissions:
    - 'create terms in writer_bio_session_status'
    - 'delete terms in writer_bio_session_status'
    - 'edit terms in writer_bio_session_status'
removing: {  }
