langcode: en
status: true
dependencies:
  enforced:
    module:
      - basic_types_events
name: 'Event Tags'
vid: event_tags
description: 'Use event tags to group events on similar topics into categories.'
weight: 0
