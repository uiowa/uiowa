<?php

/**
 * Ensure sitenow_dispatch.settings config entity exists.
 */
function sitenow_dispatch_update_9001(&$sandbox) {
  $config = \Drupal::configFactory()->getEditable('sitenow_dispatch.settings');

  if ($config->isNew()) {
    $config->set('api_key', NULL);
    $config->save();
  }
}
