services:
  sitenow_events.content_hub_api_client:
    class: Drupal\uiowa_events\ContentHubApiClient
    arguments: [ '@http_client', '@logger.channel.uiowa_events', '@cache.uiowa_events', '@config.factory' ]
  logger.channel.sitenow_events:
    parent: logger.channel_base
    arguments: [ 'sitenow_events' ]
  cache.sitenow_events:
    class: Drupal\Core\Cache\CacheBackendInterface
    tags:
      - { name: cache.bin }
    factory: cache_factory:get
    arguments: [ 'sitenow_events' ]
  # @todo Remove this.
  cache.uievents:
    class: Drupal\Core\Cache\CacheBackendInterface
    tags:
      - { name: cache.bin }
    factory: cache_factory:get
    arguments:
      - uievents
