uiowa_auth.settings:
  type: config_object
  label: 'UIowa authentication settings'
  mapping:
    role_mappings:
      label: 'Role mappings'
      type: sequence
      sequence:
        - type: string
    legacy_redirect:
      label: 'Redirect legacy paths'
      type: boolean
