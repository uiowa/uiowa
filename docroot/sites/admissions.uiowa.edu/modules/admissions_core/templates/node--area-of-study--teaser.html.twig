{%
  set classes = [
  'card--stacked ',
  'card--centered-left',
  'card--alignment-left',
  'card--enclosed',
]
%}

{% set aos_card = {
  'attributes': attributes.addClass(classes),
  'card_image': content.field_image|render,
  'card_text': content.body|render,
  'card_title': label,
  'card_link_url': url,
} %}

{% embed '@uids_base/uids/card.html.twig' with aos_card only %}
{% endembed %}
