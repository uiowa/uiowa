project:
  local:
    hostname: bands.uiowa.lndo.site
    protocol: https
  machine_name: bands
  human_name: bands.uiowa.edu
drush:
  aliases:
    local: self
    remote: bands.prod
drupal:
  db:
    database: bands_uiowa_edu
uiowa:
  requester: ewbush
