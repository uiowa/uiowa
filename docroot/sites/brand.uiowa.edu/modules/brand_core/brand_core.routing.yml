lockup.generate:
  path: '/lockup/{nid}/download'
  defaults:
    _controller: 'Drupal\brand_core\Controller\LockupController::generate'
    _title: 'Lockup Download'
  requirements:
    _permission: 'access content'
