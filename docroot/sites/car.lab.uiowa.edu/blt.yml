project:
  local:
    hostname: labcar.uiowa.ddev.site
    protocol: https
  machine_name: labcar
  human_name: car.lab.uiowa.edu
drush:
  aliases:
    local: self
    remote: labcar.prod
drupal:
  db:
    database: car_lab_uiowa_edu
uiowa:
  requester: pdeierli
