project:
  profile:
    name: sitenow
  local:
    hostname: labcfclincore.local.drupal.uiowa.edu
    protocol: https
  machine_name: labcfclincore
  human_name: cfclincore.lab.uiowa.edu
drush:
  aliases:
    local: self
    remote: labcfclincore.prod
drupal:
  db:
    database: cfclincore_lab_uiowa_edu
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true

uiowa:
  profiles:
    sitenow:
      requester: sbsngh
