project:
  local:
    hostname: clcl.local.drupal.uiowa.edu
    protocol: https
  machine_name: clcl
  human_name: clcl.uiowa.edu
drush:
  aliases:
    local: self
    remote: clcl.prod
drupal:
  db:
    database: clcl_uiowa_edu
