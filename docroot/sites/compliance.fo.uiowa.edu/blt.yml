project:
  profile:
    name: sitenow
  local:
    hostname: focompliance.local.drupal.uiowa.edu
    protocol: https
  machine_name: focompliance
  human_name: compliance.fo.uiowa.edu
drush:
  aliases:
    local: self
    remote: focompliance.prod
drupal:
  db:
    database: compliance_fo_uiowa_edu
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true

uiowa:
  profiles:
    sitenow:
      requester: dagwang
