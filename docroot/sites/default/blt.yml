project:
  local:
    hostname: default.local.drupal.uiowa.edu
    protocol: https
  machine_name: default
  human_name: default
drush:
  aliases:
    local: self
    remote: default.prod
drupal:
  db:
    database: uiowa
