project:
  local:
    hostname: local.drupal.uiowa.edu
    protocol: https
  machine_name: default
  human_name: uiowa
drush:
  aliases:
    local: self
    remote: uiowa.prod
drupal:
  db:
    database: uiowa
