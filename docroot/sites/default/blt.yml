project:
  local:
    hostname: default.uiowa.ddev.site
    protocol: https
  machine_name: default
  human_name: default
drush:
  aliases:
    local: self
    remote: default.prod
drupal:
  db:
    database: uiowa
