project:
  local:
    hostname: distance.uiowa.ddev.site
    protocol: https
  machine_name: distance
  human_name: distance.uiowa.edu
drush:
  aliases:
    local: self
    remote: distance.prod
drupal:
  db:
    database: distance_uiowa_edu
uiowa:
  requester: tripple
