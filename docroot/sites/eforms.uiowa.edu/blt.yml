project:
  profile:
    name: sitenow
  local:
    hostname: eforms.local.drupal.uiowa.edu
    protocol: https
  machine_name: eforms
  human_name: eforms.uiowa.edu
drush:
  aliases:
    local: self
    remote: eforms.prod
drupal:
  db:
    database: eforms_uiowa_edu
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true

uiowa:
  profiles:
    sitenow:
      requester: dagwang
