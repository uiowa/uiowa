project:
  profile:
    name: sitenow
  local:
    hostname: eforms.uiowa.edu
    protocol: https
  machine_name: eforms
  human_name: eforms.uiowa.edu
drush:
  aliases:
    local: self
    remote: eforms.prod
drupal:
  db:
    database: eforms_uiowa_edu
    host: localhost
    user: root
    password: null
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true
sync:
  commands:
    - 'blt:init:settings'
    - 'drupal:sync:db'
    - 'drupal:update'
    - 'sitenow:multisite:noop'
    - 'sitenow:multisite:noop'
uiowa:
  profiles:
    sitenow:
      requester: dagwang
