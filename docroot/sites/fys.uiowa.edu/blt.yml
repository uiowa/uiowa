project:
  local:
    hostname: fys.uiowa.lndo.site
    protocol: https
  machine_name: fys
  human_name: fys.uiowa.edu
drush:
  aliases:
    local: self
    remote: fys.prod
drupal:
  db:
    database: fys_uiowa_edu
uiowa:
  requester: lamba
