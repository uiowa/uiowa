project:
  local:
    hostname: gpsg.uiowa.ddev.site
    protocol: https
  machine_name: gpsg
  human_name: gpsg.uiowa.edu
drush:
  aliases:
    local: self
    remote: gpsg.prod
drupal:
  db:
    database: gpsg_uiowa_edu
uiowa:
  requester: dquam
