{%
  set classes = [
    'headline',
    'bold-headline--serif',
    'bold-headline--underline',
]
%}
<h2 {{ attributes.addClass(classes) }}>
  {%- for item in items -%}
    <span>{{ item.content }}</span>
  {%- endfor -%}
</h2>
