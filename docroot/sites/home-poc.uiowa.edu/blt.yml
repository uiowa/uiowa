project:
  local:
    hostname: home-poc.local.drupal.uiowa.edu
    protocol: https
  machine_name: home-poc
  human_name: home-poc.uiowa.edu
  profile:
    name: sitenow
drush:
  aliases:
    local: self
    remote: home-poc.dev
drupal:
  db:
    database: home_poc_uiowa_edu
cm:
  core:
    install_from_config: true
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
