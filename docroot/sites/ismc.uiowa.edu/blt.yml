project:
  local:
    hostname: ismc.uiowa.edu
    protocol: https
  machine_name: ismc
  human_name: ismc.uiowa.edu
  requester: ewbush
drush:
  aliases:
    local: self
    remote: ismc.prod
drupal:
  db:
    database: ismc_uiowa_edu
    host: localhost
    user: root
    password: null
