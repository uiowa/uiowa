project:
  local:
    hostname: nads.local.drupal.uiowa.edu
    protocol: https
  machine_name: nads
  human_name: nads.uiowa.edu
drush:
  aliases:
    local: self
    remote: nads.prod
drupal:
  db:
    database: nads_uiowa_edu
uiowa:
  requester: roggentienk
