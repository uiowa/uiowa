project:
  local:
    hostname: protostudios.local.drupal.uiowa.edu
    protocol: https
  machine_name: protostudios
  human_name: protostudios.uiowa.edu
  profile:
    name: sitenow
drush:
  aliases:
    local: self
    remote: protostudios.prod
drupal:
  db:
    database: protostudios_uiowa_edu
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true

