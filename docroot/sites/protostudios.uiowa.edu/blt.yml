project:
  local:
    hostname: protostudios.uiowa.edu
    protocol: https
  machine_name: protostudios
  human_name: protostudios.uiowa.edu
drush:
  aliases:
    local: self
    remote: protostudios.dev
drupal:
  db:
    database: protostudios_uiowa_edu
    host: localhost
    user: root
    password: null
