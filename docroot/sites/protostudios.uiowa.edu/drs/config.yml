project:
  local:
    hostname: protostudios.uiowa.ddev.site
    protocol: https
  machine_name: protostudios
  human_name: protostudios.uiowa.edu
drush:
  aliases:
    local: self
    remote: protostudios.prod
drupal:
  db:
    database: protostudios_uiowa_edu
