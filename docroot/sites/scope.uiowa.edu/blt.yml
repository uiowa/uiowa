project:
  local:
    hostname: scope.uiowa.ddev.site
    protocol: https
  machine_name: scope
  human_name: scope.uiowa.edu
drush:
  aliases:
    local: self
    remote: scope.prod
drupal:
  db:
    database: scope_uiowa_edu
uiowa:
  stage_file_proxy:
    origin: 'https://default.prod.drupal.uiowa.edu'
  requester: dquam
  site-name: SCOPE
