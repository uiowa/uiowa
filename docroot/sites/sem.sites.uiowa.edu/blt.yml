project:
  local:
    hostname: sitessem.uiowa.lndo.site
    protocol: https
  machine_name: sitessem
  human_name: sem.sites.uiowa.edu
drush:
  aliases:
    local: self
    remote: sitessem.prod
drupal:
  db:
    database: sem_sites_uiowa_edu
uiowa:
  requester: sfiddelk
