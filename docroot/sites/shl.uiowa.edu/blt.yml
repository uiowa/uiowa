project:
  local:
    hostname: shl.uiowa.edu
    protocol: https
  machine_name: shl
  human_name: shl.uiowa.edu
  profile:
    name: sitenow
drush:
  aliases:
    local: self
    remote: shl.prod
drupal:
  db:
    database: shl_uiowa_edu
    host: localhost
    user: root
    password: null
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true
sync:
  commands:
    - 'blt:init:settings'
    - 'drupal:sync:db'
    - 'drupal:update'
    - 'sitenow:multisite:noop'
    - 'sitenow:multisite:noop'
uiowa:
  profiles:
    sitenow:
      requester: makyea
