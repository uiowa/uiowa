project:
  local:
    hostname: stat.uiowa.ddev.site
    protocol: https
  machine_name: stat
  human_name: stat.uiowa.edu
drush:
  aliases:
    local: self
    remote: stat.prod
drupal:
  db:
    database: stat_uiowa_edu
uiowa:
  stage_file_proxy:
    origin: 'https://stat.prod.drupal.uiowa.edu'
