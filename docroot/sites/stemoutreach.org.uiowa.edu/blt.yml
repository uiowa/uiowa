project:
  local:
    hostname: orgstemoutreach.local.drupal.uiowa.edu
    protocol: https
  machine_name: orgstemoutreach
  human_name: stemoutreach.org.uiowa.edu
  profile:
    name: sitenow
drush:
  aliases:
    local: self
    remote: orgstemoutreach.prod
drupal:
  db:
    database: stemoutreach_org_uiowa_edu
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true

uiowa:
  profiles:
    sitenow:
      requester: tjamie
