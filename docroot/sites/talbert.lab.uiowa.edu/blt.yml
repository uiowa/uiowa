project:
  profile:
    name: sitenow
  local:
    hostname: labtalbert.local.drupal.uiowa.edu
    protocol: https
  machine_name: labtalbert
  human_name: talbert.lab.uiowa.edu
drush:
  aliases:
    local: self
    remote: labtalbert.prod
drupal:
  db:
    database: talbert_lab_uiowa_edu
cm:
  core:
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
    install_from_config: true

uiowa:
  profiles:
    sitenow:
      requester: etalbert
