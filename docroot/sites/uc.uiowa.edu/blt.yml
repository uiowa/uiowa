project:
  local:
    hostname: uc.local.drupal.uiowa.edu
    protocol: https
  machine_name: uc
  human_name: uc.uiowa.edu
  profile:
    name: sitenow
drush:
  aliases:
    local: self
    remote: uc.prod
drupal:
  db:
    database: uc_uiowa_edu
cm:
  core:
    install_from_config: true
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
uiowa:
  profiles:
    sitenow:
      requester: tripple
