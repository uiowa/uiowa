project:
  local:
    hostname: uiowa.local.drupal.uiowa.edu
    protocol: https
  machine_name: uiowa
  human_name: uiowa.edu
drush:
  aliases:
    local: self
    remote: home.test
drupal:
  db:
    database: uiowa_edu
