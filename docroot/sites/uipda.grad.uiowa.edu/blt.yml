project:
  local:
    hostname: graduipda.uiowa.lndo.site
    protocol: https
  machine_name: graduipda
  human_name: uipda.grad.uiowa.edu
drush:
  aliases:
    local: self
    remote: graduipda.prod
drupal:
  db:
    database: uipda_grad_uiowa_edu
uiowa:
  requester: klembke
