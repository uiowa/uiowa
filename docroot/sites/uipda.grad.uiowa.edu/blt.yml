project:
  local:
    hostname: uipda.grad.uiowa.edu
    protocol: https
  machine_name: graduipda
  human_name: graduipda
drush:
  aliases:
    local: self
    remote: graduipda.remote
drupal:
  db:
    database: uipda_grad_uiowa_edu
