project:
  local:
    hostname: uipress.local.drupal.uiowa.edu
    protocol: https
  machine_name: uipress
  human_name: uipress.uiowa.edu
  profile:
    name: sitenow
drush:
  aliases:
    local: self
    remote: uipress.prod
drupal:
  db:
    database: uipress_uiowa_edu
cm:
  core:
    install_from_config: true
    dirs:
      sync:
        path: profiles/custom/sitenow/config/sync
uiowa:
  profiles:
    sitenow:
      requester: athmas
