{#
/**
 * @file
 * Default theme implementation to display a one column layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 * @see https://www.drupal.org/docs/8/api/layout-api/how-to-register-layouts
 * @see https://v4-alpha.getbootstrap.com/layout/grid/
 */
#}
{%
  set classes = [
    'layout',
    'layout--onecol',
    'layout__container',
    settings.extra_classes,
  ]
%}
{{ attach_library('uids_base/fields') }}
{% if content.content %}

<div {{ attributes.addClass(classes).removeClass('layout--onecol--background') }}>

    <div class="lb__container">

      {% if region_attributes.content.storage['data-region'] %}
        <div {{ region_attributes.content.addClass('flex__wra') }}>
        {% endif %}
        {{ content.content }}
        {% if region_attributes.content.storage['data-region'] %}
        </div>
      {% endif %}

      {% if attributes.hasClass('page__container--edge-container') %}
      </div>
    {% endif %}

  </div>
</div>

{% endif %}
