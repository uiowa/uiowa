
{{ attach_library('classy/node') }}
{{ attach_library('uids_base/node-type-article') }}




{% set paragraph_card = {
      'card_classes': atrributes.addClass(['card--horizontal']),
      'card_image': event.image|render,
      'card_text': event.description_text,
      'card_author': event.date_string,
      'card_title': event.title|render,
      'card_link_url': event.url,
      'heading_level': 'h3',
    } %}

{% embed '@uids_base/uids/card.html.twig' with paragraph_card only %}
{% block card_img %}

		{# @todo Add image template #}
		<div class="card__media">
			{{ event.image }}
		</div>

{% endblock %}

{% endembed %}
