# Local DB credentials shared by all multisites, used to template
# docroot/sites/${site}/settings/local.settings.php.
#
# The per-site database name lives in docroot/sites/${site}/drs/config.yml.
drupal:
  db:
    username: db
    password: db
    host: db
    port: 3306